
module.exports = {
    runtimeCompiler: true,
    productionSourceMap: false,
    outputDir: 'dist',
    assetsDir: 'static',
    // publicPath: process.env.NODE_ENV === 'production'
    // ? '/my-project/'
    // : '/'
}